{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "PostgreSQLConnection": "Host=localhost;Port=5432;Database=LibraryDb;Username=postgres;Password=postgres"
      // Connection string setted for local host, default port and user, the LibraryDb and the password of each team member
  }
}
